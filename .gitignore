/target
.idea
.env
scratch.txt
image.tar.gz

# GCP service-account key(s) for local GA4 testing (see .env.example) -
# never committed, regardless of where they're placed.
ga4-credentials.json
*service-account*.json

# Local visitor-counter sqlite database (see src/store.rs) and its WAL/SHM
# sidecar files.
/visitors.db
/visitors.db-wal
/visitors.db-shm
/plan.md